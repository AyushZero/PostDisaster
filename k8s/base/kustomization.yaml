apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: post-disaster-alert

commonLabels:
  app.kubernetes.io/part-of: post-disaster-alert

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment-blue.yaml
  - deployment-green.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml
  - network-policy.yaml
  - resource-quota.yaml
