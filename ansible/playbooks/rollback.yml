---
- name: Roll back app container to previous image tag
  hosts: app
  become: true
  vars_files:
    - ../group_vars/all.yml
  vars:
    app_image_tag: "{{ rollback_image_tag }}"
  roles:
    - app_deploy
